<div class="col-md-8 blog-main">

  <h2><%= @blog.title %></h2>

  <% if logged_in?(:site_admin) %>
    <div class="">
      <%= link_to "Edit Item", edit_blog_path(@blog), class: 'btn btn-warning' %>
    </div>
  <% end %>

  <p>
    <%= @blog.body %>
  </p>

  <%= render 'comments/comment_form' %>
  <div id='comments' data-blog-id='<%= @blog.id %>'>
    <%= render @blog.comments %>
  </div>
</div>
