<!---start of masthead--->
<section class="jumbotron text-center">
  <div class="container">
    <h1 class="jumbotron-heading">My Portfolio</h1>
    <p class="lead text-muted">A collection of various projects I've worked on.</p>
    <p>
      <%= link_to 'About Me', about_path, class: 'btn btn-primary' %>
      <%= link_to 'My Blog', blogs_path, class: 'btn btn-primary' %>
      <%= link_to "New Portfolio Item", new_portfolio_path(@portfolio), class: 'btn btn-success' if logged_in?(:site_admin) %>
    </p>

  </div>
</section>
<!---end of masthead--->
