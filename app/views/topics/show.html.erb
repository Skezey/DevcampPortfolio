
<div class="col-sm-8 blog-main">

    <h1><%= @topic.title %></h1>

    <%= paginate @blogs %>

    <%= render @blogs %>

    <%= paginate @blogs %>

    <%= link_to "Back to blogs", blogs_path, class: "text-muted" %>

</div>
